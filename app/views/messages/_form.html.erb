<%= simple_form_for [chat, message] do |f| %>
  <%= f.input :content, label: false, required: true, placeholder: "How should I adjust my resume?" %>
  <%= f.button :submit, 'Ask', class: 'btn btn-primary float-end' %>
<% end %>
